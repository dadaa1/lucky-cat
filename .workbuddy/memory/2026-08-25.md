# 2026-08-25

## 给 data-index.json 构建增加按 updateDate 排序
- 需求：构建 `docs/data-index.json` 时按 `updateDate` 排序。
- 改动 `scripts/build-data-index.mjs`：
  - 新增 `toTime(v)` 把 updateDate/createDate 统一转成可比较毫秒时间戳（支持数字、数字字符串、以及 "YYYY-M-D H:M:S" 字符串；缺失/非法视为 0）。
  - 写文件前 `records.sort((a,b)=> toTime(b.updateDate||b.createDate) - toTime(a.updateDate||a.createDate))`，即按 updateDate 从新到旧（降序），缺失 updateDate 时回退到 createDate。
- 数据现状：updateDate 多为毫秒级时间戳数字，少数几条为 `""`（无 xiaoshujiang.json 或字段缺失）。
- 已本地重新生成 `docs/data-index.json`（153 条，验证为单调递减）。未推送（用户要求不主动 git push）。注意：CI 每次部署都会重跑该脚本，所以只需提交脚本改动即可，部署后索引自动按新顺序生成。
